import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.health.zenfit',
  appName: 'Zenfit',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
